kind = "web"
previewPath = "/"
title = "ApexRx — Pharmacy Console"
version = "1.0.0"
id = "artifacts/apexrx"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22235

[services.development]
run = "pnpm --filter @workspace/apexrx run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apexrx", "run", "build" ]
publicDir = "artifacts/apexrx/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22235"
BASE_PATH = "/"
